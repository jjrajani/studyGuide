import * as React from "react";
import "./style.scss";

export default class Dashboard extends React.Component<{}, {}> {
  // static propTypes = {}
  // static defaultProps = {}
  // state = {}

  render() {
    return (
      <div id="dashboard">
        <h1>
          Gonna be a dashbaord
        </h1>
      </div>
    );
  }
}
